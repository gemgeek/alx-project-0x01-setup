export interface PillProps {
  title: string;
}

export interface ButtonProps {
  styles: string;
  title: string;
}

export interface PostProps {
  userId: number;
  id: number;
  title: string;
  body: string;
}